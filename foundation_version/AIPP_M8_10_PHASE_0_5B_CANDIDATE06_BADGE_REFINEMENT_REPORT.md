# M8.10 Phase 0.5B Candidate 06 Badge Refinement

Application version: `0.8.10-phase0.5b-candidate.6`

- Removes the words `PYRO` and `FILTER` from Chamber badges.
- Retains only each visual icon and `× quantity`.
- Omits the Pyro badge when the Pyro count is zero.
- Omits the Filter badge when no Filter object is present.
- Reflows a lone Filter badge into the first badge position.
- Preserves Option B hover behavior, SVG namespace correction, default wheel zoom, and temporary JCEF debugging.
- Adds tests `AIPP-M810-009` and `AIPP-M810-010`.
